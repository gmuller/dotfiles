# Path
export PATH="$HOME/bin:$PATH"

# Aliases
alias battery="pmset -g batt"
alias reload!='. ~/.zshrc'
alias captainslog='vi +Goyo ~/Dropbox/logs/captains_log.txt'

#Preferred editor for local and remote sessions
if [[ -n $SSH_CONNECTION ]]; then
  export EDITOR='vim'
else
  export EDITOR='vim'
fi
